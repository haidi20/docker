node {
  def docker_django

  stage('Test app') {
    docker.image("api").inside {
      docker.image("db").inside {
        sh 'python --version'
        // sh 'python app/manage.py test ./app'
      }
    }
  }
}