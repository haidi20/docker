node {
  checkout scm

  def apiImage = docker.image("api").image("db")

  apiImage.inside {

    stage("version python") {
      sh "python --version"
    }

    stage("test django") {
      sh 'python app/manage.py test ./app'
    }
  }
}